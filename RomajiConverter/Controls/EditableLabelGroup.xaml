<UserControl x:Class="RomajiConverter.Controls.EditableLabelGroup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RomajiConverter.Controls"
             xmlns:romajiConverter="clr-namespace:RomajiConverter"
             mc:Ignorable="d"
             x:Name="Group">
    <Grid>
        <StackPanel>
            <romajiConverter:EditableLabel Text="{Binding Unit.Romaji,ElementName=Group,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}"></romajiConverter:EditableLabel>
            <Label Padding="5,0,5,5" Content="{Binding Unit.Japanese,ElementName=Group,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"></Label>
        </StackPanel>
    </Grid>
</UserControl>
